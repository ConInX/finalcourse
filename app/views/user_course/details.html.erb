<left><span>首页 > <%=@course.coursename %></span></left>
<right><span style="float:right">
    <% if session[:name] !=nil%>用户：<%= session[:name]%>   , <a href="/logout/logout" >退出</a>, 
    <% else %>
       <a href="/login/login" >登录</a>
    <%end%>
    <%= link_to '返回', :controller => 'user_course' , :action => 'user_course'  %> 
    </span></right>
    <br></br>
<table class="bordered" border = "3px" bordercolor = "0x4C96FF" align = "center">
  <thead>
    <tr>
      <th>课程名称</th>
      <th>上课地点</th>
      <th >上课时间</th>
      <th >任课老师</th>
      <th >所属学校</th>
      <th colspan="2">操作</th>
    </tr>
  </thead>

  <tbody>
    
      <tr>
        <td><%= @course.coursename %></td>
        <td><%= @course.classroom %></td>
        <td><%= @course.classtime %></td>
        <td><%= @course.tercher %></td>
        <td><%= @course.university %></td>
        <td><%= link_to '删除', :controller => 'user_course' , :action => 'delete' ,:cname =>@course.coursename %></td>
        <td><%= link_to '进入课程圈', :controller => 'article' , :action => 'article' ,:cname =>@course.coursename %></td>
      </tr>
  </tbody>
</table>